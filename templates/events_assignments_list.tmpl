  <div class="row">
    <div class="large-12 columns">
      <table class="list">
        <thead>
          <tr>
            <td>{{"User"|T}}</td>
            <td>{{"Status"|T}}</td>
            {{if .self.IsAdmin}}
            <td>{{"Actions"|T}}</td>
            {{end}}
          </tr>
        </thead>
        <tbody>
          {{$is_admin := .self.IsAdmin}}
          {{$event_id := .id}}
          {{range .list}}
          <tr>
            <td>
              <a href="/users/view/{{.UserId}}">{{.UserName}}</a>
            </td>
            <td>
              {{statusTitle "assignment" .Status|T}}
            </td>
            {{if $is_admin}}
            <td>
              {{if eq .Status -2}}
              {{else}}
              <a href="#" data-action="/assignments/cancel/{{$event_id}}?user_id={{.UserId}}">{{"Cancel"|T}}</a>
              {{end}}
              <a href="#" data-action="/assignments/delete/{{$event_id}}?user_id={{.UserId}}">{{"Delete"|T}}</a>
            </td>
            {{end}}
          </tr>
          {{end}}
        </tbody>
      </table>
    </div>
  </div>
